- clean: ['~']

- link:
    ~/.bash_profile: bash_profile
    ~/.gitconfig:
        path: gitconfig
        force: false
    ~/.ssh/config:
        path: ssh/config
        create: true
    ~/.vimrc: vimrc
    ~/bin:
        path: bin/
        create: true


